#ifndef PARAMETERS_H
#define PARAMETERS_H

#include <configuration.h>

namespace transport {

class Parameters {
public:
  Parameters(io::Configuration rundata);

private:
};

} // namespace transport

#endif